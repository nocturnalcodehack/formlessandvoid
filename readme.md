# markathas.com website built on next.js

Build the Docker image:  
docker build -t markathas-app .

Run the Docker container:  
docker run -p 3000:3000 markathas-app


addiitional indtall
brew install libomp
